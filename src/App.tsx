import React, {Fragment, useContext, useEffect} from "react";
import cities from "./assets/json/huge.json"

export default function App() {

    const ids = cities.map(city => city.id)

    return <div>hello</div>
}
